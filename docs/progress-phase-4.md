# Phase-4 进度说明

## 修复的既有偏差
- 自动模式已改为直出（不再进入编辑页）。
- 导出尺寸改为基于 148×100mm 与 PPI 的像素计算。
- 两张模板改为左右并排。
- 导入预览会按 EXIF 方向生成缓存图；导出读取原图时同样启用 EXIF 自动方向，并复用同一套布局、旋转、镜像和裁切坐标逻辑。
- 胶片栏改为真实页面缩略图。

## 本次新增交互
- 槽位交换拖拽（手柄触发）。
- 铺满裁切模式下的内容拖动构图偏移。
- 构图偏移进入 C++ 状态模型，并用于预览、缩略图、导出。

## 尚未完成（留到后续）
- 拖拽交互细节体验优化（吸附/动画提示）。
- 构图偏移的可视化辅助与重置交互。
- 自动模式更复杂策略（当前为稳定基础版）。

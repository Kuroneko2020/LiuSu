# Phase-2 进度说明

## 本次完成
- 已实现真实单图导入（文件选择器 + 槽位显示）。
- 已实现批量导入（多选、按文件名排序、跨页自动填充、不覆盖已有图片）。
- 已实现 EXIF 方向修正入口（`QImageReader::setAutoTransform(true)`）。
- 已实现槽位真实显示与状态操作（旋转、镜像、完整放入/铺满裁切）。
- 已实现胶片栏与页面状态联动（缩略图、切页、新建页、删除页）。

## 本次未完成
- 真实导出引擎。
- HEIC/HEIF 稳定完整支持（仅保留过滤与提示）。
- 槽位交换拖拽与裁切偏移手动构图。
- 设置项持久化。

## 下一阶段建议
1. 引入项目保存/恢复机制。
2. 为图片渲染增加缓存与大图性能优化。
3. 完成裁切偏移交互和槽位交换。
4. 接入导出引擎并补充自动化测试。

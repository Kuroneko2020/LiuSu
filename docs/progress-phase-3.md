# Phase-3 进度说明

## 本次做了什么
- 实现 `ExportService` 真实渲染导出（JPG/PNG、当前页/全队列）。
- 导出渲染支持槽位旋转、镜像、填充模式，并按 PPI 生成输出尺寸。
- 实现文件名规则（组合命名、日期-序号）与重名避让。
- 实现导出裁切标记开关（基础稳定版）。
- 导出页可用化：路径选择、参数生效、结果反馈。
- 实现导出相关基础设置 `QSettings` 持久化与启动恢复。

## 还没做什么
- 槽位交换拖拽。
- 铺满裁切模式下手动构图偏移。
- 自动模式完整直出。
- HEIC/HEIF 完整稳定支持。

## 下一阶段建议
1. 导出性能和批处理进度优化。
2. 裁切标记样式细化与可配置化。
3. 项目级保存/恢复与导出历史记录。
4. 拖拽交换与手动构图交互完善。
